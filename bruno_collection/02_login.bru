meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/login
  body: json
}

body:json {
  {
    "email": "test@example.com",
    "password": "Password123!"
  }
}

assert {
  res.status: eq 200
}