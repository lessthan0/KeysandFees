meta {
  name: Update Rent Payment
  type: http
  seq: 22
}

put {
  url: {{baseUrl}}/api/payments/{{paymentId}}
  body: json
}

body:json {
  {
    "rentAmount": 1200,
    "late": true,
    "notes": "Didn't pay full amount"
  }
}

assert {
  res.status: eq 200
}