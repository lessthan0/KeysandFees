meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/register
  body: json
}

body:json {
  {
    "email": "{{userEmail}}",
    "password": "{{userPassword}}",
    "role": "user"
  }
}

assert {
  res.status: eq 201
}

