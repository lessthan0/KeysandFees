meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/register
  body: json
}

body:json {
  {
    "email": "{{userEmail}}",
    "password": "{{userPassword}}"
  }
}

assert {
  res.status: eq 201
}

